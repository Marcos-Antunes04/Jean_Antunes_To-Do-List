/*import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import {createNativeStackNavigator} from '@react-navigation/native-stack'
import 'react-native-gesture-handler'
import TelaD from '../views/telad'
import TelaB from '../views/telab'








const Stack=createNativeStackNavigator()
const st = props => {
  return (
    <Stack.Navigator initialRouteName='TelaA'
    screenOptions={{headerShown: !true}}>
            
        <Stack.Screen name='TelaA'
        options={{title:'oi'}}>
             {props => (
            
             <TelaA  {...props} />
             
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaA1'
        options={{title:'Login'}}>
             {props => (

              <TelaA1  {...props}/>
             
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaA11'
        options={{title:'oi'}}>
        {props => (
            
                <TelaA11 {...props}/>
           
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaA111'
        options={{title:'oi'}}>
        {props => (
            
                <TelaA111 {...props}/>
            
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaB'
        options={{title:'oi'}} screenOptions={{headerShown: false}}>
        {props => (
            <Tab {...props} />
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaB1'
        options={{title:'oi'}}>
        {props => (
            
                <TelaB1  {...props}/>
            
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaB2'
        options={{title:'App'}}>
        {props => (
            
                <TelaB2  {...props}/>
            
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaB21'
        options={{title:'oi'}}>
        {props => (
           
                <TelaB21  {...props}/>
            
        )}
        </Stack.Screen>
        
        <Stack.Screen name='TelaC1'
        options={{title:'oi'}}>
        {props => (
            
                <TelaC1  {...props}/>
           
        )}
        </Stack.Screen>
        
        <Stack.Screen name='TelaC2'
        options={{title:'oi'}}>
        {props => (
            
                <TelaC2  {...props}/>
            
        )}
        </Stack.Screen>
        
        <Stack.Screen name='TelaC3'
        options={{title:'oi'}}>
        {props => (
            
                <TelaC3  {...props}/>
           
        )}
        </Stack.Screen>
        
        <Stack.Screen name='TelaC4'
        options={{title:'oi'}}>
        {props => (
            
                <TelaC4  {...props}/>
           
        )}
        </Stack.Screen>
        
        <Stack.Screen name='TelaC5'
        options={{title:'oi'}}>
        {props => (
            
                <TelaC5  {...props}/>
         
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaC15'
        options={{title:'oi'}}>
        {props => (
           
                <TelaC15  {...props}/>
           
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaC16'
        options={{title:'oi'}}>
        {props => (
            
                <TelaC16  {...props}/>
           
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaD1'
        options={{title:'oi'}}>
        {props => (
           
                <TelaD1  {...props}/>
          
        )}
        </Stack.Screen>

        <Stack.Screen name='TelaD'
        options={{title:'oi'}}>
        {props => (
           
                <TelaD  {...props}/>
          
        )}
        </Stack.Screen>
        
    </Stack.Navigator>
  )
}

export default st

const styles = StyleSheet.create({})*/